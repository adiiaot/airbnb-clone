export interface HouseListings {
    photos: string[],
    location: string,
    starRating: string
    favorite: boolean,
    priceAfterTax: string,
    priceBeforeTax: string,
    views: string,
    houseId: string
  }